bind1:
  hostname: server1
  remote_users:
    - user1:
      username: mysql
      auth_type: ssh-passwd
      password: mysql123
    - user2:
      username: root
      auth_type: ssh-passwd
      password: root
  groups:
    - bj_group
#  user_profiles:
#    - alex
bind2:
  hostname: server2
  remote_users:
    - user1:
      username: root
      auth_type: ssh-passwd
      password: root
  groups:
    - bj_group

bind3:
  hostname: server3
  remote_users:
    - user1:
      username: root
      auth_type: ssh-passwd
      password: root123
  groups:
    - sh_group
